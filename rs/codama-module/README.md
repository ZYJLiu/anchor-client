This example uses codama generated rust sdk directly as modules.

- need to update `crate::EXAMPLE_ID` from the generated files to `crate::<module_name>::EXAMPLE_ID;`, ex. `use crate::codama::EXAMPLE_ID;`
